name: Download text from website
on:
  push:
    branches:
      - main

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Download text file
        run: wget https://gitea.com/XIU2/TrackersListCollection/raw/branch/master/all.txt
